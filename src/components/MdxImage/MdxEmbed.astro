---
import { withBase } from "@js/pathUtils";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"embed">;

const { src, ...rest } = Astro.props;
const resolvedSrc = typeof src === "string" ? withBase(src) : src;
---

<embed src={resolvedSrc} {...rest} />
